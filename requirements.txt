# LA BUSE — dépendances cœur (MVP backend)
# Socle géo : PostGIS imposé côté base ; ici la couche connecteurs/géo en Python.

SQLAlchemy>=2.0
GeoAlchemy2>=0.14
psycopg[binary]>=3.1

pydantic>=2.5
pydantic-settings>=2.1
PyYAML>=6.0

fastapi>=0.110
uvicorn[standard]>=0.27
httpx>=0.26

shapely>=2.0
pyproj>=3.6
pyshp>=2.3

typer>=0.9
jsonschema>=4.21

# Imagerie ortho/IRC : vignettes de validation (api/ortho.py) + détection & NDVI
# (ingestion/ortho_*.py, ingestion/vegetation.py). Requis par `labuse api`.
opencv-python-headless>=4.9
numpy>=1.26

# Ingestion INSEE RP — lecture XLSX (scripts/ingest_insee_logement.py).
openpyxl>=3.1

# Dev / tests
pytest>=8.0

# Module Flash (rapport parcelle à l'unité) : PDF HTML/CSS
# (stripe retiré au BLOC B : zéro import — Lots Flash 2-4 jamais lancés ; à réintroduire
# avec le mandat paiement, les clés stripe_* de config.py restent inertes)
weasyprint>=61
jinja2>=3.1
pypdf>=5

# Cascade de juges ortho (dev/entraînement, mandat Détection Ortho) : probe DINOv2
# gelé + régression logistique. Lourd (~2 Go) et PAS requis par `labuse api` — n'est
# tiré que par `labuse ortho-juge-*`. Installer via requirements-ml.txt si besoin.
# torch>=2.2
# scikit-learn>=1.4
