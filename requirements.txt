# LA BUSE — dépendances CŒUR.
# SOURCE DE VÉRITÉ = pyproject.toml [project.dependencies]. Ce fichier installe le paquet en
# éditable → toutes les dépendances cœur suivent automatiquement (plus de liste à maintenir en
# double, plus de dérive « dépendance implicite »). `tests/test_deps_declared.py` garde l'invariant.
-e .
