# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Env / secrets
.env
# Fichiers d'environnement RÉELS du pack de déploiement (jamais committés) ;
# seuls les gabarits *.env.example sont versionnés.
deploy/env/*.env
!deploy/env/*.env.example
config/settings.yaml

# Data (never commit ingested public data dumps / fixtures volumineuses)
data/*
# …sauf le dataset Obsimmo fourni par le client (petit, versionné, requis par la fiche LOT 4-C)
!data/obsimmo_vente_reunion.json
# …et l'extrait La Réunion de la carte des loyers DHUP (open data, petit, requis par la fiche LOT 4-B)
!data/carte_loyers_reunion_2025.json
# …et l'extrait INSEE statut d'occupation RP 2022 (open data, petit, requis par la fiche LOT 4-B)
!data/insee_occupation_reunion_2022.json
*.gpkg
*.shp
*.shx
*.dbf
*.prj
*.cpg
*.tif
*.tiff
/tmp/

# OS / editor
.DS_Store
.idea/
.vscode/

# Aperçus / captures générés (livrés via l'app / SendUserFile, pas du source)
labuse_radar*.svg
shot_*.png
data/plu_saint_paul/*.pdf
data/plu_saint_paul/*.txt
# PDF sources PLU (lourds, ré-édités) : provenance tracée dans les YAML config/plu_*.yaml
docs/plu_sources/*.pdf

# Sauvegardes DB (jamais committées)
backups/
*.dump

# Copie serveur du Caddyfile (le modèle est deploy/Caddyfile.example)
deploy/Caddyfile

# Phase 1.A GPU test — cache brut régénérable (réponses API Carto)
reports/phase1a_gpu/cache/

# Captures d'audit QA (artefacts locaux)
qa/shots/

# Sources externes brutes (PDF PLH, etc.) — non commitées
.sources/
import_all.log
