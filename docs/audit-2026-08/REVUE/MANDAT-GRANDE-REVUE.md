# MANDAT — GRANDE REVUE AVANT MISE EN LIGNE
Régime AUTONOME du début à la fin. Aucun checkpoint, aucune question : CC tranche seul, note ses arbitrages, une ligne chacun. Commit par lot (R1→R10). RÈGLES COMMUNES. App en route (exceptions encadrées aux lots R5 et R10). Findings RV-001→, gravité 🔴🟠🟡. Rapport : docs/audit-2026-08/REVUE/RAPPORT.md. Doctrine intacte : Sourcé/Estimé/Absent, moteur unique, jamais de faux positif — AUCUN chiffre servi ne change dans ce mandat (seule exception : le re-run encadré de R10).

## R1 — FRAÎCHEUR DES 59 SOURCES
Pour chacune des sources de data_sources : millésime/date amont la plus récente RÉELLEMENT disponible chez le producteur (vérifie en ligne : data.gouv, geoservices IGN, ADEME, Géorisques, DGFiP, Sitadel, BODACC, INPI…), vs ce qui est ingéré. Tableau : source · ingéré · disponible · retard · commande d'ingestion existante O/N. Ingère les retards QUAND la commande existe et que la source est libre (licences en attente : constate seulement). Toute ingestion est versionnée (IngestionRun) et notée. Si l'amont est indisponible/cassé : constate, n'invente pas. Marque en fin de tableau : les sources qui alimentent la cascade et qui ont bougé (déclencheur du R10).

## R2 — CRON & FUSEAUX
Inventaire de tous les jobs périodiques existants ou prévus (avis-echeance, veilles, digest, backups, ingestions, purges sessions…) : ce qui existe, ce qui est cronable mais jamais lancé, ce qui manque. Table docs/EXPLOITATION-CRON.md : job · commande exacte · cadence recommandée · dépendances · à installer sur le VPS.
FUSEAUX (bug consigné) : la machine est en CEST, PostgreSQL à l'heure Réunion (+2) — la porte quota partenaires se réinitialise au lieu de lever entre 20h et minuit CEST. Corrige : toute fenêtre temporelle métier (quotas, gels, digests, « aujourd'hui ») doit être calculée sur un fuseau EXPLICITE Indian/Reunion, jamais sur l'heure locale implicite du process. Cherche systématiquement (datetime.now, date.today, CURRENT_DATE, now() sans tz) et corrige ce qui borne une fenêtre métier. Tests de non-régression sur la porte quota.

## R3 — MOTEUR UNIQUE REJOUÉ
Rejoue le protocole LOT AP sur le run servi actuel (q_v11_m137) : la même parcelle par tous les chemins (fiche, carte, export, PDF, Copilote, comparateur, projets) → mêmes chiffres. 150 parcelles tirées au sort + les cas connus. Toute divergence = 🔴 corrigée dans ce mandat. Vérifie aussi le point noté au canari : des parcelles à score élevé classées a_creuser (ex ICPE/PPR) — l'affichage client doit rendre le POURQUOI lisible (motif de non-opportunité visible sur la fiche), sans changer le classement.

## R4 — SÉCURITÉ (avant exposition Internet)
Audit et correction de ce qui est sûr : secrets (aucun en dur, .env seulement, aucun dans le git log), injections (tout SQL paramétré — grep f-strings dans les text()), en-têtes HTTP (CSP raisonnable, X-Content-Type-Options, X-Frame-Options, HSTS prêt pour le VPS), cookies (HttpOnly/SameSite/Secure-ready), CORS strict, uploads (types/tailles validés), rate limit sur login et routes coûteuses (constaté A5 — vérifie), dépendances (pip-audit + npm audit : corrige les patchs sans breaking change, documente le reste), erreurs (aucune stack trace servie au client). Scanner aussi le bundle front buildé (dist/) : aucune clé secrète (sk_, rk_, xkeysib-, tokens) ne doit y figurer — seules les clés publiques (pk_) sont tolérées côté client ; grep du dist + test. Purger les console.log/console.debug du front et les print() de debug du backend hors CLI — la console d'un client en production doit être muette ; les logs légitimes passent par le logger. NE TOUCHE PAS : le mécanisme d'auth lui-même (argon2id, tokens — audité A1, sain), la 2FA admin (mandat VPS). Chaque durcissement = un test.

## R5 — VITESSE
Mesure d'abord : baseline p50/p95 des familles de routes (patron LOT AM), pages front (temps de rendu carte, fiche, dashboard). Identifie les 10 pires. Optimise UNIQUEMENT ce qui ne change aucun chiffre servi : index manquants (EXPLAIN avant/après), requêtes N+1, caches courts sur lectures pures (patron /stats/entonnoir), payloads (champs inutiles, compression), lazy-loading front, bundle (code-splitting si gain net). Interdit : toucher au moteur de calcul, dégrader la fraîcheur des données, cache > 5 min sur données métier. Chaque optimisation : mesure avant/après au rapport. Exception app : redémarrages backend autorisés pour tester (relance systématique, jamais laissé mort).

## R6 — PAIEMENT STRIPE BOUT EN BOUT
Si STRIPE_RESTRICTED_KEY et clés test présentes dans .env : déroule en mode TEST le cycle complet — souscription (checkout), renouvellement simulé, échec de carte (cartes de test Stripe), passage past_due, suspension manuelle dashboard, rétablissement, rapprochement Stripe⇄comptes (alerte orphelins). Preuves : captures + événements. Si clés absentes : vérifie tout ce qui est vérifiable sans (mode « non configuré » propre partout, aucun crash, aucun envoi fantôme) et liste ce qui reste à tester en réel — RV dédié 🟡. WEBHOOKS : si l'app expose un endpoint de webhook Stripe, la signature (Stripe-Signature, endpoint secret en .env) DOIT être vérifiée avant tout traitement — teste avec une signature invalide (rejet attendu). Si aucun webhook n'existe : le dire au rapport, et noter que le passage au VPS en exigera un (paiements asynchrones).

## R7 — MAILS
Si les IDs de templates Brevo sont dans .env : envoie RÉELLEMENT chaque template (essai, souscription, onboarding 1/2/3, carte refusée, suspension, rétablissement) vers l'adresse de Vic — pas d'invention de destinataires — et vérifie la réception + le rendu des variables. Sinon : transport journalisé (patron avis-echeance), vérifie que chaque déclencheur produit le bon contenu au bon moment, et liste ce qui reste à tester en réel — RV dédié 🟡. Vérifie les rappels J+3/J+10 sur les fiches (dates simulées OK sur comptes de test [REVUE-TEST], purgés en fin).

## R8 — RECETTE DASHBOARD
La Tour de contrôle en conditions réelles : chaque section, chaque bouton, chaque LED. Les capteurs D1 doivent être alimentés par l'usage réel des derniers jours (usage_events non vides, heartbeats, conso IA attribuée). Vérifie : chiffres du Pilotage exacts (recoupés SQL), âge du backup juste, badge sources cohérent avec R1, courrier (cycle Demandé→Imprimé→Posté), essai 48h (création + bascule à date forcée sur compte [REVUE-TEST]), suspension/rétablissement. Le 403 admin déjà gelé par tests — vérifie qu'ils passent encore.

## R9 — NETTOYAGE
- Suite backend : les 15 failed + 9 erreurs de collection pré-existants — répare ou retire (avec justification unitaire) ; objectif suite VERTE (skips documentés tolérés). Plus aucun échec non expliqué.
- Les 17 🟡 en dette (3 cycle 5 + 14 FIX-C6) : traite ce qui coûte < 30 min pièce, re-documente le reste avec sa raison (dette assumée, datée).
- AC-003 : pose les FK ON DELETE CASCADE sur les 11 tables à compte_id qui n'en ont pas (patron GB-063 : purge orphelins d'abord, migration idempotente, garde de régression).
- Code mort : routes 0-consommateur (croise l'inventaire M54-INV), composants front orphelins, imports inutiles, fichiers *.orig/*.bak, branches locales mortes (liste seulement — Vic supprime).
- Données : purge [GB-TEST] restants (inventaire cycle 6) + tout résidu [AUDIT-TEST]/[REVUE-TEST], vérifiée SQL zéro orphelin. Vestiges scoring : identifie les tables mortes (abuse_scores, parcel_v_score, score_e, score_snapshot*… — croise avec le code) → liste À SUPPRIMER proposée au rapport, NE SUPPRIME PAS (décision Vic).
- Traçabilité : pole_echange 19/61 et tva_primo 13/13 sans data_source_id → rattache. division_or_candidates périmée (q_v10) → régénère si commande existe, sinon documente.

## R10 — RE-RUN FINAL (conditionnel)
UNIQUEMENT si R1 a ingéré des sources qui alimentent la cascade. Suis docs/BASCULE_RUN_RUNBOOK.md à la lettre : nouveau label q_v12_m137 via scripts/run_ile_q_v11.sh adapté (24 communes, --no-resume), PAS de réécriture du run servi. Puis : dryrun-report sur 2 communes (canari), score-v2 --run-id q_v12_m137 (obligatoire : p_score_v2_runs), bascule des 2 pointeurs, npm run build, build-mvt, vérif cartouche+LED+fiche 200. purge-runs-morts : dry-run seulement, la purge --apply reste à Vic (app arrêtée). Si aucune source cascade n'a bougé : note-le, pas de re-run.

## FIN
Critères : gardées G1-G6 vertes · tsc 0 · build ✓ · suite backend verte (ou chaque échec justifié nominativement) · zéro régression visuelle · comptes [REVUE-TEST] purgés vérifiés SQL · aucun chiffre servi modifié hors R10. Rapport final : tableau fraîcheur, table CRON, baseline vitesse avant/après, findings RV-xxx corrigé/documenté, liste tables à supprimer (décision Vic), reste-à-tester (Stripe/Brevo si clés absentes). Compte-rendu « Demandé → traité » par lot + commande de merge en dernier élément isolé (git merge --no-ff audit/grande-revue). Tu ne merges pas.
