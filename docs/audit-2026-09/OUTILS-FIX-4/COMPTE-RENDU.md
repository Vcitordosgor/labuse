# OUTILS-FIX-4 — compte-rendu

Branche `fix/outils-4` (depuis `main` à jour, FIX-3 mergé). 4 commits, un par lot, poussés, **pas de merge**.

- `addbfbc8` **Lot A — Scan patrimoine** : A1 millésime MAJIC (`parcelle_personne_morale.millesime` = 2025) en tête de Possession ; A2 `valorisation_nu_eur` remontée du tiroir vers le bloc de synthèse (4ᵉ carte, statut Estimé) ; A3 dates Sit@del année-seule (`AAAA-01-01`, 83 % des permits) rendues « 2024 » et non « 01/01/2024 » ; A4 le nombre nu final = le **code `etat_dau` brut** (2/4/5/6), pas un compte — servi comme libellé `autorisé/achevé` (DAACT/etat_dau=6, doctrine `permits.py`/`pc_caducs`), sans inventer de mot pour 2/4/5.
- `f1120eab` **Lot B — Étudier un bien** : B1 établi — « Prix du secteur » = `sector_price` (multi-type, 5 ans récents, rayon) vs « Détail par type » = `reference_locale` (mono-type, **toutes années**, rayon) : deux moteurs/fenêtres/périmètres, chaque cellule dit son rayon + note d'écart ; B2 badges Sourcé/Estimé (SHAB=Estimé, terrain=Sourcé, prix sortie=Estimé, terrain-zone=Sourcé) ; B3 « Dernières ventes de cette parcelle » (`dvf_mutations_parcelle`, 1 ligne/mutation) + état vide honnête.
- `3d87d67a` **Lot C — Pièges & risques** : C1 tiroir « Comment ce score est calculé » (seuils **lus de `cascade_rules.yaml` + barème back**, jamais en dur au front) ; C2 colonne CATNAT au crible (`catnat_arretes`, grain commune, n + dernier arrêté, Sourcé) ; C3 doublon vérifié sur `97415000AC0024`.
- `cbb6488a` **Lot D — Courrier** : D1 état du service + D2 plafond du jour (`/courrier/statut` étendu : `plafond_jour/envoyes_jour/reste_jour`), affichés à l'ouverture avant toute saisie.

**B1 — ce qui a été établi.** `secteur_bati` = `sector_price` (bilan.py:200) : bâti multi-type appartement→mixte, période **5 ans récents** préférée, rayon adaptatif 500→1500 m, exclusion aberrants. `par_type` = `reference_locale` (bilan.py:288) : **un seul type**, **aucune fenêtre temporelle** (toutes les ventes), même rayon adaptatif, trim 5 %. Sur `97411000AH0100` : 2 800 €/m² (129 v, 2022-2025) vs 2 672 (193 v, toutes années) — deux méthodes, pas une contradiction. Décision : garder les deux, rendre fenêtre + périmètre lisibles (aucune médiane retirée).

**C3 — ce qui a été établi.** Sur `97415000AC0024`, la couche `risques` émet un `SOFT_FLAG` **par polygone `georisque_alea`** intersecté ; la donnée DEAL est fragmentée (3 polygones mvt : faible/moyen/eleve, mais le run servi en portait deux « moyen »). Résultat : « Aléa mouvement_terrain — niveau moyen » **deux fois** — le même fait, deux zonages. Le score consolidé prend le **max**, il n'était donc **pas** gonflé : bruit de présentation seul. Corrigé au **point de lecture du crible** (`_diligence_dossier`, dédup des lignes strictement identiques — comme le fait déjà `O5Servitudes`) ; l'engine de cascade et le run golden ne sont **pas** touchés. Vérifié après fix : « niveau moyen » apparaît une fois.

**Ce qui a résisté / décisions prises.** A4 : la nomenclature Sit@del des codes 2/4/5 n'est pas dans le dépôt → j'ai refusé d'inventer un mot par code et servi le binaire `autorisé/achevé` que l'app affirme déjà via DAACT (`veille_promoteurs.py:131`). B3 : `dvf_mutations_parcelle` sert plusieurs lignes par vente (Maison + Dépendance) → dédup par `id_mutation` (`scoreur.py`). C2 : `catnat_arretes` absente en base de test → garde `to_regclass` (sinon 500 sur labuse_test).

**Tests.** tsc 0 · vitest **187/187** · pytest **2689 passed, 8 failed, 39 skipped**. Les 8 échecs sont **PRÉ-EXISTANTS** (identiques sur `HEAD` sans mes changements, vérifié par `git stash`) : `test_courrier_boucle`×2 + `test_dashboard::test_ia_log` (erreurs SQLAlchemy base de test), `test_front_reliquats::test_r5_etudier_deux_marges` (assertion obsolète — la chaîne « la charge calibrée » est absente **à HEAD aussi**), `test_run_serving_coherence::test_tuiles_mvt`, `verrous/{lot1,lot2,lot4}` (sur base réelle). Mon seul régression (test_servitudes cassé par le CATNAT non gardé) est corrigée → suite passe de 2688 à 2689.

**Captures** avant/après par écran touché dans `avant/` et `apres/` : Scan Possession, Scan Construction, Étudier (constat + ventes), crible (+ CATNAT), Courrier — app réelle (base `labuse`), vérifiées au DOM.
