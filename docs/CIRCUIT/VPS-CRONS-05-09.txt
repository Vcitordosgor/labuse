# Lecture seule du VPS labuse-vps — 05/09/2026 (CIRCUIT-1 lot 0.1)
# Commandes autorisées par le mandat : ls/cat /etc/cron.d, crontab -l, ls/cat /opt/labuse/state/jobs

===== ls -la /etc/cron.d/ =====
total 72
drwxr-xr-x   2 root root  4096 Aug 28 17:01 .
drwxr-xr-x 118 root root 12288 Sep  2 06:41 ..
-rw-r--r--   1 root root   102 Mar 31  2024 .placeholder
-rw-r--r--   1 root root   201 Apr  8  2024 e2scrub_all
-rw-r--r--   1 root root  1145 Aug 27 18:34 labuse-abuse
-rw-r--r--   1 root root  1812 Aug 27 18:34 labuse-backup
-rw-r--r--   1 root root  1367 Aug 27 18:34 labuse-ban
-rw-r--r--   1 root root  1152 Aug 27 18:34 labuse-bodacc
-rw-r--r--   1 root root  1278 Aug 27 18:34 labuse-dpe
-rw-r--r--   1 root root  1331 Aug 27 18:34 labuse-dvf
-rw-r--r--   1 root root  1246 Aug 27 18:34 labuse-fraicheur
-rw-r--r--   1 root root  2146 Aug 27 18:34 labuse-notifications
-rw-r--r--   1 root root  1019 Aug 27 18:34 labuse-radar
-rw-r--r--   1 root root   844 Aug 27 18:34 labuse-sessions
-rw-r--r--   1 root root  1910 Aug 27 18:34 labuse-sitadel
-rw-r--r--   1 root root   396 Jan 10  2024 sysstat

===== cat /etc/cron.d/labuse-* =====
# MANDAT VPS (27/08/2026) — HORAIRES EN HEURE RÉUNION : le VPS est passé en fuseau système
# Indian/Reunion (timedatectl) ; les horaires ci-dessous sont locaux (ex-UTC décalés de +4 h).
# Wave Adresses (Lot 3) — scan QUOTIDIEN des patterns de scraping (séquences d'IDU,
# régularité machinale, volume nocturne, ratio consultations/exports) sur la journée
# écoulée → abuse_scores + alertes admin. AUCUN blocage automatique (faux positifs) :
# le gel est une décision manuelle de Vic (table acces_gels).
# 06:00 UTC (10:00 Réunion) — INCHANGÉ M98 : hors chemin critique.
# Verrou flock (-E 200 = tour sauté, jamais confondu avec un échec du job) ; saut journalisé.
#
# Installation VPS :  install -o root -g root -m 644 deploy/cron.d/abuse /etc/cron.d/abuse
SHELL=/bin/sh
0 10 * * * labuse flock -n -E 200 /run/lock/labuse-abuse.lock -c "set -a; . /etc/labuse/labuse.env; set +a; cd /opt/labuse/app && /opt/labuse/venv/bin/labuse abuse-scan >> /var/log/labuse/abuse_scan.log 2>&1"; test $? -eq 200 && echo "$(date -u +\%FT\%TZ) abuse : passage sauté : exécution précédente en cours" >> /var/log/labuse/abuse_scan.log
# MANDAT VPS (27/08/2026) — HORAIRES EN HEURE RÉUNION : le VPS est passé en fuseau système
# Indian/Reunion (timedatectl) ; les horaires ci-dessous sont locaux (ex-UTC décalés de +4 h).
# LA BUSE — sauvegarde PostgreSQL quotidienne (pré-vol M7 · P4)
# Dump -Fc + rotation (deploy/scripts/backup_postgres.sh) ; le rapatriement vers le
# poste local est un cron LOCAL (pull_backups.sh), jamais une tâche du VPS.
# M98 : 05:30 UTC (09:30 Réunion) — APRÈS toutes les écritures de la nuit (sitadel 02:15,
# bodacc 02:30, notifications 03:00, ban le 5 à 03:30, dérivés hebdo lundi 03:45).
# 2e ligne M98 : la maintenance hebdo (VACUUM ANALYZE + audit tailles, db_maintenance.sh,
# dimanche 04:00 UTC = 08:00 Réunion) sort du crontab utilisateur (qui reste VIDE — un seul
# mécanisme, Phase 4) et vit ici, avec le backup : VACUUM avant dump, log sous /var/log/labuse/
# pour entrer dans la rotation logrotate.
# Verrou flock partagé (-E 200 = tour sauté, jamais confondu avec un échec) ; saut journalisé.
#
# Installation VPS :  install -o root -g root -m 644 deploy/cron.d/backup /etc/cron.d/backup
SHELL=/bin/sh
30 9 * * * labuse flock -n -E 200 /run/lock/labuse-backup.lock -c "set -a; . /etc/labuse/labuse.env; set +a; /opt/labuse/app/deploy/scripts/backup_postgres.sh >> /var/log/labuse/backup.log 2>&1"; test $? -eq 200 && echo "$(date -u +\%FT\%TZ) backup : passage sauté : exécution précédente en cours" >> /var/log/labuse/backup.log
0 8 * * 0 labuse flock -n -E 200 /run/lock/labuse-backup.lock -c "set -a; . /etc/labuse/labuse.env; set +a; /opt/labuse/app/deploy/scripts/db_maintenance.sh >> /var/log/labuse/maintenance.log 2>&1"; test $? -eq 200 && echo "$(date -u +\%FT\%TZ) maintenance : passage sauté : exécution précédente en cours" >> /var/log/labuse/maintenance.log
# MANDAT VPS (27/08/2026) — HORAIRES EN HEURE RÉUNION : le VPS est passé en fuseau système
# Indian/Reunion (timedatectl) ; les horaires ci-dessous sont locaux (ex-UTC décalés de +4 h).
# Wave Adresses (Lot 1) — refresh MENSUEL de la Base Adresse Nationale 974 (la BAN bouge :
# adressage communal continu). Export officiel départemental (Licence Ouverte), remplacement
# complet idempotent + rattachement parcelles (point-dans-parcelle, cad_parcelles, < 20 m)
# + index inverse adresse_parcelles (assiette multi-parcelles, bâtiments à cheval)
# + rattachement des copropriétés RNIC restées sans parcelle.
# Passage le 5 à 03:30 UTC (07:30 Réunion) — INCHANGÉ M98 : mensuel, hors chemin critique du
# digest (les ingestions quotidiennes sitadel/bodacc tournent désormais à 02:15/02:30).
# Verrou flock (-E 200 = tour sauté, jamais confondu avec un échec du job) ; saut journalisé.
#
# Installation VPS :  install -o root -g root -m 644 deploy/cron.d/ban /etc/cron.d/ban
SHELL=/bin/sh
30 7 5 * * labuse flock -n -E 200 /run/lock/labuse-ban.lock -c "set -a; . /etc/labuse/labuse.env; set +a; cd /opt/labuse/app && /opt/labuse/venv/bin/labuse ingest-ban --download >> /var/log/labuse/ban_refresh.log 2>&1"; test $? -eq 200 && echo "$(date -u +\%FT\%TZ) ban : passage sauté : exécution précédente en cours" >> /var/log/labuse/ban_refresh.log
# MANDAT VPS (27/08/2026) — HORAIRES EN HEURE RÉUNION : le VPS est passé en fuseau système
# Indian/Reunion (timedatectl) ; les horaires ci-dessous sont locaux (ex-UTC décalés de +4 h).
# LA BUSE — BODACC quotidien (J+2) : procédures collectives des SIREN propriétaires (upsert
# idempotent), puis chaîne des dérivés légers. Jamais les tables de run.
# M98 : 02:30 UTC (06:30 Réunion) — AVANT les notifications de 03:00, pour que le digest de
# 7h Réunion parle de la nuit, pas de l'avant-veille.
# Verrou flock (-E 200 = tour sauté, jamais confondu avec un échec du job) ; saut journalisé.
#
# Installation VPS :  install -o root -g root -m 644 deploy/cron.d/bodacc /etc/cron.d/bodacc
SHELL=/bin/sh
30 6 * * * labuse flock -n -E 200 /run/lock/labuse-bodacc.lock -c "set -a; . /etc/labuse/labuse.env; set +a; cd /opt/labuse/app && /opt/labuse/venv/bin/labuse ingest-bodacc >> /var/log/labuse/bodacc.log 2>&1 && /opt/labuse/venv/bin/labuse fraicheur-derives >> /var/log/labuse/bodacc.log 2>&1"; test $? -eq 200 && echo "$(date -u +\%FT\%TZ) bodacc : passage sauté : exécution précédente en cours" >> /var/log/labuse/bodacc.log
# MANDAT VPS (27/08/2026) — HORAIRES EN HEURE RÉUNION : le VPS est passé en fuseau système
# Indian/Reunion (timedatectl) ; les horaires ci-dessous sont locaux (ex-UTC décalés de +4 h).
# LA BUSE — DPE ADEME hebdo (J+2) : flux continu, upsert numero_dpe idempotent ; le compteur de
# réveil du badge en réserve (F/G ∩ mono ∩ non-écarté ≥ 200) est réévalué par les dérivés.
# Mardi 05:20 UTC (09:20 Réunion) — INCHANGÉ M98 : hebdo, hors chemin critique du digest.
# NOTE : l'ingestion (~10-20 min) chevauche le backup de 05:30 le mardi — pg_dump prend un
# instantané cohérent ; l'état complet ingéré est dans le dump du lendemain.
# Verrou flock (-E 200 = tour sauté, jamais confondu avec un échec du job) ; saut journalisé.
#
# Installation VPS :  install -o root -g root -m 644 deploy/cron.d/dpe /etc/cron.d/dpe
SHELL=/bin/sh
20 9 * * 2 labuse flock -n -E 200 /run/lock/labuse-dpe.lock -c "set -a; . /etc/labuse/labuse.env; set +a; cd /opt/labuse/app && /opt/labuse/venv/bin/labuse ingest-dpe >> /var/log/labuse/dpe.log 2>&1 && /opt/labuse/venv/bin/labuse fraicheur-derives >> /var/log/labuse/dpe.log 2>&1"; test $? -eq 200 && echo "$(date -u +\%FT\%TZ) dpe : passage sauté : exécution précédente en cours" >> /var/log/labuse/dpe.log
# MANDAT VPS (27/08/2026) — HORAIRES EN HEURE RÉUNION : le VPS est passé en fuseau système
# Indian/Reunion (timedatectl) ; les horaires ci-dessous sont locaux (ex-UTC décalés de +4 h).
# LA BUSE — DVF hebdo-en-attente-du-semestriel (J+2) : détection Last-Modified des CSV Etalab ;
# no-op tant que la livraison (avril/octobre) n'a pas changé ; sinon reload du millésime modifié.
# Mercredi 05:00 UTC (09:00 Réunion) — INCHANGÉ M98 : hebdo, dérivés seulement, hors chemin
# critique du digest. NOTE : le jour d'une livraison semestrielle, le reload (~5-10 min) peut
# dépasser le backup de 05:30 — pg_dump prend un instantané cohérent ; l'état complet ingéré
# est dans le dump du lendemain.
# Verrou flock (-E 200 = tour sauté, jamais confondu avec un échec du job) ; saut journalisé.
#
# Installation VPS :  install -o root -g root -m 644 deploy/cron.d/dvf /etc/cron.d/dvf
SHELL=/bin/sh
0 9 * * 3 labuse flock -n -E 200 /run/lock/labuse-dvf.lock -c "set -a; . /etc/labuse/labuse.env; set +a; cd /opt/labuse/app && /opt/labuse/venv/bin/labuse refresh-dvf >> /var/log/labuse/dvf.log 2>&1 && /opt/labuse/venv/bin/labuse fraicheur-derives >> /var/log/labuse/dvf.log 2>&1"; test $? -eq 200 && echo "$(date -u +\%FT\%TZ) dvf : passage sauté : exécution précédente en cours" >> /var/log/labuse/dvf.log
# MANDAT VPS (27/08/2026) — HORAIRES EN HEURE RÉUNION : le VPS est passé en fuseau système
# Indian/Reunion (timedatectl) ; les horaires ci-dessous sont locaux (ex-UTC décalés de +4 h).
# LA BUSE — LA SENTINELLE DE FRAÎCHEUR (M84) : un décrochage doit se DIRE (jamais six
# semaines de silence). `labuse check-fraicheur` sort en code 1 si une source dépasse 2× sa
# cadence → cron mail. M98 (Phase 4) : sortait du crontab utilisateur (doc de déploiement),
# devient le 10e fichier cron.d — un seul mécanisme, le crontab utilisateur reste vide.
# 06:30 UTC (10:30 Réunion) — APRÈS tout le Train J+1 : la sentinelle juge la nuit complète.
# Verrou flock (-E 200 = tour sauté, jamais confondu avec un échec du job) ; saut journalisé.
#
# Installation VPS :  install -o root -g root -m 644 deploy/cron.d/fraicheur /etc/cron.d/fraicheur
SHELL=/bin/sh
30 10 * * * labuse flock -n -E 200 /run/lock/labuse-fraicheur.lock -c "set -a; . /etc/labuse/labuse.env; set +a; cd /opt/labuse/app && /opt/labuse/venv/bin/labuse check-fraicheur >> /var/log/labuse/check_fraicheur.log 2>&1"; test $? -eq 200 && echo "$(date -u +\%FT\%TZ) fraicheur : passage sauté : exécution précédente en cours" >> /var/log/labuse/check_fraicheur.log
# MANDAT VPS (27/08/2026) — HORAIRES EN HEURE RÉUNION : le VPS est passé en fuseau système
# Indian/Reunion (timedatectl) ; les horaires ci-dessous sont locaux (ex-UTC décalés de +4 h).
# LA BUSE — LE CENTRE DE NOTIFICATIONS (M85). Le digest quotidien part à 7h00 HEURE RÉUNION.
# Le VPS tourne en UTC : 03:00 UTC = 07:00 à La Réunion (UTC+4, pas d'heure d'été). Le fuseau est
# AUSSI explicite dans le code (events.REUNION_TZ = UTC+4) — le cron et le code s'accordent, aucun
# n'hérite du fuseau machine.
# M98 : horaire INCHANGÉ — mais les ingestions quotidiennes passent désormais AVANT (sitadel
# 02:15, bodacc 02:30) : le digest voit les données de la nuit, plus celles de l'avant-veille.
#
# Chaîne quotidienne, dans l'ordre : évaluer les suivis de parcelle → évaluer les veilles Copilote
# (nouvelles données → notifications) → signaler les décrochages de fraîcheur (source en retard →
# notif systeme pilote) → purger les notifications de plus de 90 j → envoyer le digest e-mail
# (filtré par préférence, digest vide ne part pas, désinscription + préférences dans chaque
# e-mail). Chaque geste est idempotent et journalisé.
# Verrou flock (-E 200 = tour sauté, jamais confondu avec un échec du job) ; saut journalisé.
#
# Installation VPS :  install -o root -g root -m 644 deploy/cron.d/notifications /etc/cron.d/notifications
SHELL=/bin/sh
0 7 * * * labuse flock -n -E 200 /run/lock/labuse-notifications.lock -c "set -a; . /etc/labuse/labuse.env; set +a; cd /opt/labuse/app && /opt/labuse/venv/bin/labuse evaluer-suivis >> /var/log/labuse/notifications.log 2>&1 && /opt/labuse/venv/bin/labuse evaluer-veilles >> /var/log/labuse/notifications.log 2>&1 && /opt/labuse/venv/bin/labuse notifier-fraicheur >> /var/log/labuse/notifications.log 2>&1 && /opt/labuse/venv/bin/labuse purge-notifications >> /var/log/labuse/notifications.log 2>&1 && /opt/labuse/venv/bin/labuse digest --freq quotidien >> /var/log/labuse/notifications.log 2>&1"; test $? -eq 200 && echo "$(date -u +\%FT\%TZ) notifications : passage sauté : exécution précédente en cours" >> /var/log/labuse/notifications.log
# MANDAT VPS (27/08/2026) — HORAIRES EN HEURE RÉUNION : le VPS est passé en fuseau système
# Indian/Reunion (timedatectl) ; les horaires ci-dessous sont locaux (ex-UTC décalés de +4 h).
# BLOC B (B3) — radar des sources : sondes HEAD/métadonnées (zéro téléchargement), hebdo.
# Le radar SIGNALE (healthz/crons + page Sources) ; l'ingestion reste une décision humaine.
# M98 : lundi 02:00 UTC (06:00 Réunion) — la sonde amont passe AVANT tout le Train J+1.
# Verrou flock (-E 200 = tour sauté, jamais confondu avec un échec du job) ; saut journalisé.
#
# Installation VPS :  install -o root -g root -m 644 deploy/cron.d/radar /etc/cron.d/radar
SHELL=/bin/sh
0 6 * * 1 labuse flock -n -E 200 /run/lock/labuse-radar.lock -c "set -a; . /etc/labuse/labuse.env; set +a; cd /opt/labuse/app && /opt/labuse/venv/bin/labuse radar-sources >> /var/log/labuse/radar.log 2>&1"; test $? -eq 200 && echo "$(date -u +\%FT\%TZ) radar : passage sauté : exécution précédente en cours" >> /var/log/labuse/radar.log
# MANDAT VPS (27/08/2026) — HORAIRES EN HEURE RÉUNION (fuseau système Indian/Reunion).
# LA BUSE — purge des sessions expirées (R9) : `sessions_auth` garde la vérité des sessions
# utilisateur (token haché) ; les lignes échues ne servent plus qu'à grossir la table.
# DELETE WHERE expire_at < now(), quotidien. Verrou flock (-E 200 = tour sauté).
#
# Installation VPS :  install -o root -g root -m 644 deploy/cron.d/sessions /etc/cron.d/sessions
SHELL=/bin/sh
0 8 * * * labuse flock -n -E 200 /run/lock/labuse-sessions.lock -c "set -a; . /etc/labuse/labuse.env; set +a; cd /opt/labuse/app && /opt/labuse/venv/bin/labuse purge-sessions >> /var/log/labuse/purge_sessions.log 2>&1"; test $? -eq 200 && echo "$(date +\%FT\%T\%z) purge-sessions : passage sauté : exécution précédente en cours" >> /var/log/labuse/purge_sessions.log
# MANDAT VPS (27/08/2026) — HORAIRES EN HEURE RÉUNION : le VPS est passé en fuseau système
# Indian/Reunion (timedatectl) ; les horaires ci-dessous sont locaux (ex-UTC décalés de +4 h).
# LA BUSE — SITADEL quotidien-en-attente-du-mensuel (J+2) : le refresh delta (recouvrement
# 3 mois) est un no-op léger tant que le SDES n'a pas publié ; le jour de la livraison
# mensuelle, l'ingestion part le jour même. Puis dérivés (hebdo le lundi : + m10 vélocité).
# M98 : ingestion 02:15 UTC (06:15 Réunion) — AVANT les notifications de 03:00, pour que le
# digest de 7h Réunion parle de la nuit, pas de l'avant-veille. Le volet dérivés hebdo passe
# au lundi 03:45 UTC (07:45 Réunion) : toujours APRÈS l'ingestion du lundi (02:15), et
# désormais AVANT le backup (05:30) pour que le dump du jour capte ses écritures.
# Verrou flock partagé entre les deux lignes (-E 200 = tour sauté) ; saut journalisé.
#
# Installation VPS :  install -o root -g root -m 644 deploy/cron.d/sitadel /etc/cron.d/sitadel
SHELL=/bin/sh
15 6 * * * labuse flock -n -E 200 /run/lock/labuse-sitadel.lock -c "set -a; . /etc/labuse/labuse.env; set +a; cd /opt/labuse/app && /opt/labuse/venv/bin/python -m labuse.ingestion.permits_sdes --refresh >> /var/log/labuse/sitadel_refresh.log 2>&1 && /opt/labuse/venv/bin/labuse fraicheur-derives >> /var/log/labuse/sitadel_refresh.log 2>&1"; test $? -eq 200 && echo "$(date -u +\%FT\%TZ) sitadel : passage sauté : exécution précédente en cours" >> /var/log/labuse/sitadel_refresh.log
45 7 * * 1 labuse flock -n -E 200 /run/lock/labuse-sitadel.lock -c "set -a; . /etc/labuse/labuse.env; set +a; cd /opt/labuse/app && /opt/labuse/venv/bin/labuse fraicheur-derives --hebdo >> /var/log/labuse/sitadel_refresh.log 2>&1"; test $? -eq 200 && echo "$(date -u +\%FT\%TZ) sitadel (hebdo) : passage sauté : exécution précédente en cours" >> /var/log/labuse/sitadel_refresh.log

===== crontab -l (ubuntu) =====
no crontab for ubuntu

===== ls /opt/labuse/state/jobs/ =====
ls: cannot access '/opt/labuse/state/jobs/': No such file or directory
